{
    "$schema":"http://json-schema.org/schema",
    "$id":"RootStateSchematics",
    "title": "Root State Schema",
    "description": "Add the Root State to Your App Module",
    "properties": {
        "stateFolderName": {
            "type": "string",
            "description": "The name of the folder for the state",
            "default": "state",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What do you want the name of your state folder to be?"
        },
        "stateInterfaceName": {
            "type": "string",
            "description": "The State Interface Name",
            "default": "AppState",
            "x-prompt": "The name for the root state interface?"

        },
        "addToAppModule": {
            "type": "boolean",
            "description": "Add To Root Module",
            "default": "true",
            "x-prompt": "Add the StoreModule to the App Module"
        }
        
    },
    "required": [
       
    ]
}